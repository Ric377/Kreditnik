<resources>

    <!-- Основная тема -->
    <style name="Theme.Kreditnik" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- твои цвета и настройки -->
    </style>

    <!-- Тема сплэш-экрана -->
    <style name="Theme.Kreditnik.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background_light</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Kreditnik</item>
    </style>

</resources>
